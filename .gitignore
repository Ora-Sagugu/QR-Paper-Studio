# 【规则怎么写？】
# - 写一行文件夹名/  → 忽略整个文件夹
# - 写 *.pyc          → 忽略所有以 .pyc 结尾的文件（* 表示任意名字）
# =============================================================================

# .venv/  你本机用 python -m venv 创建的虚拟环境
# 里面装了 Flask 等一大堆包，体积大，而且每人电脑路径不同，传上去没意义。
# 别人克隆项目后自己 pip install -r requirements.txt 就行。
.venv/

# __pycache__/  Python 运行 .py 时自动生成的“编译缓存”文件夹
# 加快下次启动用的，删了也能再生成，不必进仓库。
__pycache__/

# *.pyc  单个缓存文件（有时不在 __pycache__ 里也会出现）
*.pyc

# .env  常用来放密码、API 密钥等敏感配置
# 一旦推到 GitHub 公开仓库，别人可能看到，非常危险，所以一定要忽略。
.env

# .DS_Store  苹果 Mac 系统在文件夹里自动生成的隐藏文件，和项目无关。
.DS_Store

# 可选：编辑器/IDE 个人配置文件夹（如果你用 VS Code、PyCharm 等开发工具，它们会在项目里生成一些配置文件夹）
.vscode/
.idea/
